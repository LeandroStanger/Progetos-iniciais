const nomes = ['João', 'MARIA', 'carlos', 'Ana', 'LUCAS'];

if (nomes.length >= 1) {
    nomes[0] = nomes[0].toLowerCase();
}

if (nomes.length >= 2) {
    nomes[1] = nomes[1].toLowerCase();
}

if (nomes.length >= 3) {
    nomes[2] = nomes[2].toLowerCase();
}

if (nomes.length >= 4) {
    nomes[3] = nomes[3].toLowerCase();
}

if (nomes.length >= 5) {
    nomes[4] = nomes[4].toLowerCase();
}

console.log(nomes)